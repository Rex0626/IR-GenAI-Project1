# ============== Python 自身的快取與編譯檔案 ==============
# 這些是 Python 執行時自動產生的，不需要上傳
__pycache__/
*.pyc
*.pyo
*.pyd

# ============== 虛擬環境資料夾 (非常重要！) ==============
# 這些資料夾包含了整個 Python 解釋器和所有套件，體積巨大且因電腦而異
# 絕對不能上傳！ requirement.txt 才是用來重建環境的。
dynamic_env/
static_env/
venv/
.venv/
env/

# ============== 測試工具產生的快取 ==============
# Pytest 會用這個來加速測試，是暫存檔
.pytest_cache/

# ============== 爬蟲產生的資料與日誌 ==============
# 這些檔案可以透過重新執行爬蟲來生成，避免將大量數據上傳到 Git
dual_source_scraper/data/
dual_source_scraper/logs/

# ============== 分析報告與圖表 ==============
# 這些也是可以透過執行分析腳本重新生成的
compare/reports/

# ============== IDE 或系統產生的設定檔 ==============
# 例如 VS Code 的設定檔，這些通常是個人化的
.vscode/
.idea/

# 其他工具或系統產生的檔案
CACHEDIR.TAG
*.DS_Store